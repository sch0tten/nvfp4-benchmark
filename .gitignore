# Secrets — never commit (see CLAUDE.md SECRETS OVERRIDE)
.env
.env.*
*.env
# but DO track the template (it holds no secret):
!.env.example

# Python / venv
__pycache__/
*.pyc
.venv/
.venv-*/

# Large artifacts / raw run logs (keep curated results/ in git, not raw dumps)
results/raw/
*.safetensors
*.bin
models/
